# Verification

## Current policy and candidate

The [0.3.0 five-gate tracker](../tasks/2026-09-05-backwriter-0.3.0-independent-namespace-complete-view.md)
records implementation evidence; Protocol, address model and CLI authority own
semantics. Gates 1–5 are complete after Gate 5R's readiness **GO** on candidate
`4c3038505b8f87d01483b60abd3ec7494d0bc94d`. Gate 5's GNU/musl **293/293 tests**
and 52 release smokes are reused after complete input equality, not rerun.
Gate 5R first passed pipe/Unicode/EOF/error/split-Help/delivery preflight, then
ran four new independent arms once each. All sixteen final files match the
fixed oracle. Both N arms receive complete Paragraph Content without recovery;
newline mistakes and Wrong Apply are zero. Stream bytes and whole-arm time
are independent of exact model-delivered JSON text-content bytes. Unobservable
platform-added envelopes are explicitly excluded, not counted as zero.
Version selection is deferred; Cargo/CLI/public stay `0.2.6` with no release.

The final executed inputs, binary/log identities, independent oracle, four
unaltered transcripts and capture gaps are retained in the
[Gate 5 record](../tasks/2026-09-05-backwriter-0.3.0-independent-namespace-complete-view.md#gate-5-d3-and-final-readiness--no-go).
The earlier Genie N's ninth command was optional baseline File verification,
not missing-Content recovery or an automatic BOX 26 failure. Its actual count,
NO-GO and raw records remain intact. Capture source, four complete records and
recomputation rules are in the
[Gate 5R record](../tasks/2026-09-05-backwriter-0.3.0-independent-namespace-complete-view.md#gate-5r-result--go).
Only docs changed after the native matrix. N GNU alone was reconstructed and
matched its recorded SHA; the existing N-1 GNU binary matched its pinned SHA.
No full suite, musl rebuild, old benchmark or native macOS/Windows run is claimed.

Cargo and source-built Version remain `0.2.6`. Published N-1 Source Authority is
`09bb6c424081594bd86a95f04345b786ef9b46b6`, closed `0.2.6`; the candidate
includes later namespace, optional batch projection, direct shell View and Help
changes. It is not byte-identical to N-1. CLI relocation is not source equality.

During development run affected namespace, shell View, Help and module-wiring
tests. Run the full GNU/musl matrix at the stable Gate 5 candidate, not at every
small gate. Record executed, reused and unexecuted evidence separately. Reuse
requires equality of production, tests/fixtures, build scripts, Cargo/lock,
toolchain, target, features, profile and relevant flags. Source equality alone
does not suffice. Version/build-metadata changes require binary identity and
Version KAT checks, not repetition of historical benchmarks.

GNU `x86_64-unknown-linux-gnu` is the local development target. Canonical Linux
release verification uses the separately installed
`x86_64-unknown-linux-musl` target with pinned Rust/Cargo 1.95.0; the toolchain
file does not auto-install it. Build/test/host execution is not publication.

## Required current correctness matrix

| Boundary | Required evidence |
| --- | --- |
| v5 foundation | Canonical File/Paragraph/Line KATs and strict decode/error priority; no v3/v4 decoder or parallel constructor; shared source hash/length/Line count, false-count NotCurrent and checked arithmetic |
| Source safety | Admission and exact logical paths, capability-relative no-follow access, ordinary-file/type boundaries, UTF-8/NUL and late read/resource failures; no arbitrary semantic size cap, partial success, retry, history or relocation |
| Namespace | Exact root .bw and legacy .artext/bw exclusion with platform case rules; ordinary siblings/nested paths; task-local old sentinels unchanged; private abnormal file/symlink not exposed; Help/Version do not create private state |
| Observation | One accepted forward observation or contracted matching Host range read; raw observer has no StructuralCursor; one structural cursor and Issuer; checked offsets and 8,191/8,192/8,193 boundaries; no complete auxiliary source/corpus retention |
| Search | Literal tiers, canonical order, duplicates, exact File lookup, late-failure all-or-none; one reusable canonical address scratch and no second result/JSON collection |
| View | Single and ordered batch self/upward projection, duplicates, normal RelationAbsent, all-or-none failure, one observation per source group; exact empty/Unicode/None/LF/CR/CRLF Content |
| Direct shell View | One single call for one input or one batch call for plural input, optional common/self projection; input/fresh refs and exact byte framing; no Content-only Search/View/re-resolution or display clone |
| Ref/output failures | Reserve before output/append; absent consumes no slot; append before each projected record; exact L+k on write failure, L+P on flush failure, terminal Stream and no BufWriter Drop retry; no delivery or publication rollback claim |
| Edit/Apply | Body-only Line and exact File/Paragraph Content, argv/one-shot EOF stdin priority, three receipt cases, exact no-op, five raw Edit/four Position forms, staging/rename/uncertain failures and source/proof/Anchor preservation or invalidation |
| Check/Host/Anchor | Ordered currentness and Current-only fresh slots; same-source stale rejection; Host hit/miss/mismatch/invalidation and zero-I/O hit; same-after Anchor reflection and path-exact fail-close |
| Pick/Data/raw Session | Stable subsequence and multiplicity, actual predicates, typed lifetime-bound storage, binding/index/clone/borrowed Apply, error continuation and status precedence; no implicit capability workflow |
| Adapter | Independent exact Help/usage/output KATs, prefix options, stdout/stderr/exit separation, Help before Runtime/download/private IO, streamed v2/v1 envelopes, quoted executable examples and unchanged non-Help output |
| Drift | Correct 1 / Safe Reject 6 / Wrong Apply 0 in Untrusted and correctly guarded Host; duplicate Paragraph rejection, no automatic recovery |

Store creation/read/write/spill/cleanup remain N/A: no consumer exists. Do not
create IO to fill a checklist or inspect actual historical state. Capacity
overflow proves recoverable Resource handling, not process allocator exhaustion.
Native macOS/Windows/PowerShell/CMD and publisher locking, rollback, fsync and
crash-durability guarantees remain unverified or absent.

## Gate 4 executed evidence

Entry is clean `main = origin/main =
3d35f14338d2374777acd485d0bce49387800fbc`. The existing 74 CLI functions
remain one integration crate, now grouped by Help, Edit, View, Check and Shell.
The three binary writer/reservation tests remain distinct. New Help assertions
extend existing tests; no independent test function or fixture framework is added.

The final focused GNU run passes **116 distinct tests**: 74 CLI, 3 binary
writer/reservation, 21 View, 13 Runtime View and 5 namespace controls. This is an
impacted-component matrix, not a full GNU product suite. Gate 3's earlier
116/9-smoke and N-1's 285/285 results are historical baselines, not new full-suite
claims. Independent Help KATs include four advanced topics and complete direct
and raw examples; top-level one-shot advanced execution remains rejected.

Offline/locked metadata/tree, fmt, GNU all-target check, clippy with warnings
denied and release build pass using a task-local target. The tracker records
release Help/Version and parent/candidate exact stdout, stderr, exit and final
source-byte comparisons. Core/Runtime, v5, schemas, Cargo/lock/toolchain and
dependencies are unchanged by this gate. Only Help lookup changes a production
function body; remaining function bodies are preserved across private moves.

No full GNU/musl, AI arm, historical performance rerun or benchmark is claimed.
No server, installed bw, actual HOME, public root or operational state is touched.

## Commands and repository hygiene

Development Gate 4 uses the existing default features/profiles:

```sh
cargo metadata --offline --locked --format-version 1
cargo tree --offline --locked
cargo fmt --all -- --check
cargo check --offline --locked --all-targets
cargo test --offline --locked --bin bw --test cli
cargo clippy --offline --locked --all-targets -- -D warnings
cargo build --offline --locked --release
```

Add the focused Runtime/View/namespace filters recorded in the tracker. At
Gate 5 run full GNU tests and the corresponding musl target commands:

```sh
cargo test --offline --locked
rustc +1.95.0 --print cfg --target x86_64-unknown-linux-musl
cargo check --offline --locked --all-targets --target x86_64-unknown-linux-musl
cargo test --offline --locked --target x86_64-unknown-linux-musl
cargo clippy --offline --locked --all-targets --target x86_64-unknown-linux-musl -- -D warnings
cargo build --offline --locked --release --target x86_64-unknown-linux-musl
```

Use task-local targets/fixtures and only authorized tools. These commands create
no release authority. Before staging inspect exact paths/diff and empty index,
check links/anchors/fences/conflict markers, confirm repository `.bw` and
`.artext` absent/untracked, and reject tracked output. Preserve unrelated
changes, planning and historical records. Stage only Owner-authorized exact
paths, repeat cached diff checks, then verify clean HEAD/remote equality after
non-force push. Remove only inspected task-owned temporary paths.

## Gate 5 comparison and evidence links

Compare pinned N-1 above with the actual N candidate. Locate BOX 25–26's
original independent four-file fixture first; if unavailable, declare a shared
spec-conformant fixture digest and independent full-byte oracle. Required shape:
eight duplicate Lines, four primary edits (one per file), four untouched
secondary Lines and LF/CR/CRLF/None.

Run Dummy N-1/N and Genie N-1/N as four arms. Dummy chooses freely from public
Help; one-shot is not failure. Genie's reference is Search 1 + Paragraph batch
View 1 + Replace 4 + Check batch 1 + File batch View 1: eight capability commands,
excluding Help and shell lifecycle. Candidate's first context View must supply
decision-making Content with zero Content-only self-View/named-copy/raw-View
workaround, terminator mistakes or Wrong Apply and an exact final oracle.
Record N-1 deficiencies honestly. Do not generalize one edit per file to repeated
same-source edits.

Separate process count, actual commands, model tool turns, unexpected CLI
failures, Content-only commands, stdout/stderr bytes, model-visible bytes and
elapsed. More useful Content is not itself regression. Each n=1 arm gives
observations, not a timing ratio gate or broad performance claim. Do not execute
0.2.5 or older comparisons, external-tool arms or the user-installed 0.2.2 bw.

- [N tracker and Gate 4 preservation mapping](../tasks/2026-09-05-backwriter-0.3.0-independent-namespace-complete-view.md)
- [N-1 0.2.6 evidence](../tasks/2026-09-04-backwriter-0.2.6-operational-adapter-verification-contraction.md)
- [Historical evidence index](../history/index.md)

Historical numbers, environments, SHAs, path spellings and conditions remain
unchanged in those records and the verbatim excerpts. The index replaces
per-release narratives here; it does not promote old evidence to current passes.
